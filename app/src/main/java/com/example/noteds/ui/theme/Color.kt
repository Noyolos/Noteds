package com.example.noteds.ui.theme

import androidx.compose.ui.graphics.Color

// Main Brand Colors (Midnight Blue & Vibrant Orange)
val MidnightBlue = Color(0xFF1A237E) // Primary
val VibrantOrange = Color(0xFFFF6D00) // Secondary
val BackgroundLightBlue = Color(0xFFF5F6FA) // Background
val CardSurface = Color(0xFFFFFFFF) // Surface

// Functional Colors
val FunctionalRed = Color(0xFFD50000)
val FunctionalGreen = Color(0xFF00C853)

// Aliases for backward compatibility
val PrimaryColor = MidnightBlue
val SecondaryColor = VibrantOrange
val BackgroundColor = BackgroundLightBlue
val DebtColor = FunctionalRed
val PaymentColor = FunctionalGreen

// Text Colors
val TextPrimary = MidnightBlue // Main text
val TextSecondary = Color(0xFF757575)
val TextWhite = Color(0xFFFFFFFF)
