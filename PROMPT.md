直接拿下面这段当 prompt 丢给 Codex / Cursor / 任何 AI 助手就行（它会基于你仓库里的源码写一份完整架构说明）：

---

Prompt：

你现在扮演一名资深软件架构师，请基于整个项目源码，写一份完整的架构说明文档，对象是「有一定 Android/后端基础的开发者」，文档语言用简体中文，技术名词可以保留英文。

该项目是一个叫 Noteds 的安卓记账 / 催收工具，基于 Kotlin + Jetpack Compose + MVVM + Room。主要场景：家里的杂货店 / 小生意，用来记录客户赊账与回款，按客户/分组管理，后续会扩展到云端同步和 Web 仪表盘，方便在电脑查看数据和自动备份。

> 非常重要：

1. 先「阅读并理解整个代码仓库」后再写文档，不要凭空瞎编没有出现在代码里的东西。


2. 如果有推断或未来扩展设想，请明确标注为「未来规划 / 推测」，不要和现有实现混在一起。




文档请按以下结构输出（标题格式示例）：


---

1. 项目概览（Project Overview）

用 1–2 段话说明：

Noteds 的核心用途（记账 / 催收 / 赊账管理）

典型用户是谁（例如：店家母亲在手机本地记账，子女通过云端 / Web 仪表盘查看）

当前版本支持的核心能力（本地记账、客户分组、报表统计、备份恢复等）


简要说明目前是单机本地为主，但设计上预留了未来云端同步 + Web 仪表盘的扩展方向。



---

2. 整体架构（System Architecture）

2.1 分层结构

说明项目的分层 / 模块划分，例如：

UI 层（Jetpack Compose Screen / Navigation）

ViewModel 层（状态管理 + 业务逻辑）

Repository 层（数据访问抽象）

Data 层（Room、实体、DAO、备份工具）


指出各层之间的依赖关系：
UI → ViewModel → Repository → Room / 其他数据源。


2.2 主要模块与包结构

列出关键 package / module（例如 ui, viewmodel, data, db, backup, reports 等），并概括每个包负责的职责。

如果有自定义导航栈 / 工具类（例如 screenStack）也在这里点名说明。


2.3 系统视角图（文字描述即可）

用文字描述一个「系统架构图」：

Android App（本地 Room）

未来的 Cloud API / Web Dashboard（如果在代码或注释里有提到，顺带说明规划）


清晰区分：现在已经实现的部分 和 未来计划扩展的部分。



---

3. 领域模型与数据结构（Domain Model & Data Schema）

3.1 核心实体（Entities）

聚焦以下实体，分别说明字段和业务含义（结合实际代码）：

Customer / CustomerEntity

id, name, isGroup, parentId, nextDueDate, deleted, 头像/证件照片相关字段等

说明分组与客户共用同一张表，通过 isGroup + parentId 实现层级结构


LedgerEntry

id, customerId, type（欠款/回款）, amount, date, note, createdAt 等

说明一笔欠款和一笔回款如何在模型中表示



如果还有其他重要数据结构（例如统计结果、报表 DTO 等），也一并说明。


3.2 Room 数据库设计

描述 Room 数据库的版本（例如 version 6）和主要表：

customers 表结构 + 关键索引

ledger_entries 表结构 + 关键索引


解释数据库迁移逻辑：

从 v1→v5、v5→v6 做了哪些 schema 变更（例如增加分组字段、索引、标记字段等）。

如何保证历史数据兼容。



3.3 层级结构与分组汇总

说明 parentId + isGroup 如何实现「文件夹/分组 + 子客户」的层级结构。

解释 CustomerViewModel.getCustomers 或类似函数：

如何计算某个分组下所有子客户的总欠款 / 总回款

递归遍历的逻辑（如果代码中有）




---

4. 功能架构（Feature Architecture）

4.1 导航与页面结构

描述底部导航：

「首页 / 客户 / 报表」三个主入口


说明自定义的页面栈 / 层级导航：

列表 → 分组 → 客户详情 → 编辑 / 添加

如何管理返回栈（例如使用自建 screenStack 而非官方 Navigation）



4.2 客户管理与账务录入

说明创建/编辑/删除客户或分组的流程：

创建客户 / 分组时如何写入 Room

初始欠款如何自动生成一条账目记录

删除客户时如何递归删除子客户和对应账目


单条账务记录（LedgerEntry）的新增 / 修改 / 删除流程。


4.3 报表与统计逻辑

详细解释 ReportsViewModel 或相关统计模块：

如何计算：

总待收款（总欠款 - 总回款）

当月新增欠款 / 回款

近 N 个月的月度趋势

账龄分布（0–30、31–60、61–90、90+ 等）

交易次数、平均回款周期

Top 欠款人（按当前净欠款额排序前 N 名）



如果代码中已经为首页提供趋势图数据序列，也说明数据生成方式。


4.4 首页仪表盘（DashboardScreen）

说明首页展示的各个卡片：

总待收款、趋势图、当月新增欠款/回款、Top 欠款人列表等


交互说明：

点击 Top 欠款人跳转至客户详情的流程

与 ViewModel / Repository 的数据流关系




---

5. 备份与恢复（Backup & Restore）

描述当前项目是如何把数据序列化为 JSON 备份文件：

包含哪些字段（customers + ledger_entries）

是否保留原始 ID / 时间戳


说明从备份恢复时的逻辑：

如何清空旧数据 / 覆盖数据

如何重置自增 ID，避免和原始 ID 冲突

如何保证层级关系（parentId）不丢失


如果有本地文件存储位置、文件命名规则，也一并说明。



---

6. 未来的云端同步与 Web 仪表盘规划（基于代码与注释的合理推断）

> 如果仓库中已有关于「同步到服务器 / Web 仪表盘」的注释或实验代码，请基于实际内容说明；
如果没有，请用**清晰标注为“未来规划”**的方式，提出一个与当前结构自然契合的扩展方案。



6.1 同步策略（Cloud Sync Strategy，未来规划）

推荐的做法：

以当前 JSON 备份格式作为「全量 snapshot」

通过 HTTP POST /api/snapshot 上传到服务器

服务器按 owner/device 维度清空旧数据并重建


说明为什么这样设计对于单设备 + 家庭用记账（母亲记账、子女查看）足够简单可靠。


6.2 云端存储与自动备份（未来规划）

简要描述：

服务器数据库 schema（customers / ledger_entries + ownerId/deviceId）

定期数据库备份、导出能力（为了防止手机/服务器故障）



6.3 Web 仪表盘（Dashboard Web App，未来规划）

说明 Web 端大致会包含的页面与功能：

总览 Dashboard：总待收款、月度趋势、账龄分布、Top 欠款人等图表

客户列表+搜索+筛选

单客户详情及账目时间线


标明哪些统计可以直接复用手机端现有逻辑，哪些更适合在服务器端计算。



---

7. 非功能性考量（Non-functional Concerns）

简要说明（基于代码实际情况）：

性能与数据规模预期（适用于小商户的本地账本）

错误处理与数据一致性（例如备份/恢复的异常情况）

未来若要扩展用户/权限/多设备同步时，架构层面需要注意的点。




---

8. 总结（Summary）

用一小节总结：

Noteds 目前已经完成的本地账本架构优势（结构清晰、可扩展、支持分组与报表）

预留的扩展空间（云端同步、Web 仪表盘、家庭多人协同等）

这个架构适合的典型使用场景（例如家庭店铺赊账管理、线下小生意）




---

输出要求：

结构清晰、层级明确，标题使用 Markdown（### 等）格式。

以实际代码为主，少用空洞的形容词；重点解释「是怎么实现的」以及「为什么这样设计」。

如果某个部分在代码中还没有实现，但从命名 / 注释可以推断出意图，请明确标注为「推测 / 未来规划」，不要包装成已实现功能。



---

说明：

你只需要把上面的 prompt 整段复制，
在 Codex / Cursor 里打开 Noteds 的 repo，然后发这段，让它先读代码再写文档就可以了。
后面如果你要「精简版」「英文版」或者「只写云端架构部分」，再改动提示里的要求就行。
